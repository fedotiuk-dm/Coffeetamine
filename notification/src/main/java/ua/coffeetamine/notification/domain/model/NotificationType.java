package ua.coffeetamine.notification.domain.model;

/** Mirror of the OpenAPI {@code NotificationType} enum — keep the values in sync. */
public enum NotificationType {
  PING_RECEIVED,
  MATCH_CREATED
}
